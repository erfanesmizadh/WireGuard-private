🔥 اسکریپت نصب خودکار WireGuard (تانل خصوصی ایران ↔ خارج)

یک اسکریپت Bash حرفه‌ای برای نصب و راه‌اندازی سریع WireGuard بین سرور ایران و سرور خارج.

این اسکریپت به صورت تعاملی از شما اطلاعات لازم را می‌پرسد، کلیدها را ایجاد می‌کند، فایل کانفیگ را می‌سازد و تونل را به صورت خودکار فعال می‌کند.

---

🚀 امکانات

✅ نصب خودکار WireGuard
✅ ساخت خودکار کلیدهای Private/Public
✅ تنظیم تعاملی (Iran / Outside)
✅ پشتیبانی IPv4 و IPv6
✅ ساخت خودکار فایل کانفیگ
✅ فعالسازی خودکار سرویس (systemd)
✅ مناسب برای تانل ایران به خارج

---

📦 پیش‌نیازها

- سیستم عامل Ubuntu یا Debian
- دسترسی root
- داشتن IP عمومی روی هر دو سرور

---

⚡ نصب

ابتدا ریپازیتوری را کلون کنید:

`git clone https://github.com/erfanesmizadh/WireGuard-private.git`

`cd WireGuard-private`

`chmod +x install.sh`

اجرای اسکریپت:

sudo bash install.sh

---

🧠 مراحل تنظیم

در زمان اجرای اسکریپت سوالات زیر از شما پرسیده می‌شود:

- نوع سرور (iran یا outside)
- کلید عمومی سرور مقابل
- IP داخلی تونل
- IP سرور مقابل در تونل
- IPv6 (اختیاری)
- پورت WireGuard
- آدرس Endpoint

بعد از وارد کردن اطلاعات:

1️⃣ WireGuard نصب می‌شود
2️⃣ کلیدها ساخته می‌شوند
3️⃣ فایل کانفیگ ایجاد می‌شود
4️⃣ تونل فعال می‌شود
5️⃣ سرویس دائمی می‌شود

---

🔎 مشاهده وضعیت تونل

sudo wg show

---

🔄 ریستارت تونل

روی سرور ایران:

sudo systemctl restart wg-quick@wg-iran

روی سرور خارج:

sudo systemctl restart wg-quick@wg-outside

---

❗ رفع مشکلات رایج

اگر اتصال برقرار است ولی ping انجام نمی‌شود:

چک کردن route:

ip route

غیرفعال کردن rp_filter:

sysctl -w net.ipv4.conf.all.rp_filter=0

---

👨‍💻 سازنده

erfanesmizadh

---

⭐ حمایت

اگر این پروژه برای شما مفید بود، لطفاً در گیت‌هاب به آن Star بدهید ⭐
